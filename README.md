# kafka 体験だ！

## overview

### 前提

* docker入ってること
* docker-compose入ってること

### 使い方

```
$ docker-compose -f kafka/docker-compose.yml up -d
$ ./mvnw package
$ docker-compose up
```

以上！
